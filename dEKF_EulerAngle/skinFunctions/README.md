To compute the total NORMAL forces using the skin under the feet use the
function `totalForceFromSkinData()`. e.g.:
`totalForceFromSkinData(totalForceFromSkinData('backwardTipping', 4, 'left'))`









